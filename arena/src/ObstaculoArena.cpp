#include "ObstaculoArena.h"

// Aquí le damos el cuerpo al constructor (asignar las variables y listo)
ObstaculoArena::ObstaculoArena(float startX, float startY, float w, float h) {
    posicion.x = startX;
    posicion.y = startY;
    ancho = w;
    alto = h;
}

